<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:jee="http://www.springframework.org/schema/jee"
       xmlns:task="http://www.springframework.org/schema/task"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context
           http://www.springframework.org/schema/context/spring-context.xsd
           http://www.springframework.org/schema/tx
           http://www.springframework.org/schema/tx/spring-tx.xsd
           http://www.springframework.org/schema/jee
           http://www.springframework.org/schema/jee/spring-jee.xsd
           http://www.springframework.org/schema/task
           http://www.springframework.org/schema/task/spring-task.xsd">

    <!-- Load external properties -->
    <context:property-placeholder location="classpath:settlement.properties"
                                  ignore-unresolvable="true"/>

    <!-- Component scanning (exclude controllers, handled by MVC config) -->
    <context:annotation-config/>
    <bean class="org.springframework.orm.jpa.support.PersistenceAnnotationBeanPostProcessor"/>
    <context:component-scan base-package="com.settlement">
        <context:exclude-filter type="annotation"
                                expression="org.springframework.stereotype.Controller"/>
        <context:exclude-filter type="regex"
                                expression="com\\.settlement\\.ejb\\..*"/>
    </context:component-scan>

    <!-- JNDI DataSource from WebSphere -->
    <jee:jndi-lookup id="dataSource" jndi-name="jdbc/OracleDS" expected-type="javax.sql.DataSource"/>

    <!-- JPA EntityManagerFactory (JTA-managed for EAR/MDB compatibility) -->
    <bean id="entityManagerFactory"
          class="org.springframework.orm.jpa.LocalContainerEntityManagerFactoryBean">
        <property name="jtaDataSource" ref="dataSource"/>
        <property name="packagesToScan" value="com.settlement.entity"/>
        <property name="jpaVendorAdapter">
            <bean class="org.springframework.orm.jpa.vendor.HibernateJpaVendorAdapter">
                <property name="database" value="ORACLE"/>
                <property name="showSql" value="false"/>
            </bean>
        </property>
        <property name="jpaProperties">
            <props>
                <prop key="hibernate.dialect">org.hibernate.dialect.OracleDialect</prop>
                <prop key="hibernate.hbm2ddl.auto">validate</prop>
                <prop key="hibernate.format_sql">true</prop>
                <prop key="hibernate.transaction.jta.platform">org.hibernate.engine.transaction.jta.platform.internal.WebSphereLibertyJtaPlatform</prop>
            </props>
        </property>
    </bean>

    <!-- JTA Transaction Manager: participates in container-managed transactions (MDB) -->
    <bean id="transactionManager" class="org.springframework.transaction.jta.JtaTransactionManager"/>

    <!-- Enable @Transactional -->
    <tx:annotation-driven transaction-manager="transactionManager"/>

    <!--
        Dedicated executor for settlement retry processing.
        Uses ThreadPoolTaskExecutor with explicit bounds to prevent unbounded thread
        growth from long-running Thread.sleep backoff retries.

        Async threads start fresh JTA transactions per @Transactional call, so
        container-managed threads are not required here.

        corePoolSize    : threads kept alive even when idle
        maxPoolSize     : upper bound when queue is full
        queueCapacity   : bounded queue; backpressure before spawning extra threads
        keepAliveSeconds: idle non-core thread TTL
    -->
    <bean id="settlementExecutor"
          class="org.springframework.scheduling.concurrent.ThreadPoolTaskExecutor">
        <property name="corePoolSize"     value="5"/>
        <property name="maxPoolSize"      value="20"/>
        <property name="queueCapacity"    value="100"/>
        <property name="keepAliveSeconds" value="60"/>
        <property name="threadNamePrefix" value="settlement-retry-"/>
        <!--
            CallerRunsPolicy: when queue is full, the submitting thread runs the task
            directly instead of rejecting it. Provides backpressure without losing tasks.
        -->
        <property name="rejectedExecutionHandler">
            <bean class="java.util.concurrent.ThreadPoolExecutor.CallerRunsPolicy"/>
        </property>
    </bean>

    <!--
        Dedicated executor for alert webhook HTTP calls.
        Uses ThreadPoolTaskExecutor (not container-managed) because alerts
        do not participate in JTA transactions.

        corePoolSize    : threads kept alive even when idle
        maxPoolSize     : upper bound when queue is full
        queueCapacity   : bounded queue; backpressure before spawning extra threads
        keepAliveSeconds: idle non-core thread TTL
    -->
    <bean id="alertExecutor"
          class="org.springframework.scheduling.concurrent.ThreadPoolTaskExecutor">
        <property name="corePoolSize"     value="2"/>
        <property name="maxPoolSize"      value="5"/>
        <property name="queueCapacity"    value="50"/>
        <property name="keepAliveSeconds" value="30"/>
        <property name="threadNamePrefix" value="alert-webhook-"/>
        <!--
            DiscardPolicy: silently drop overflow alerts rather than blocking
            the settlement retry thread. Alert loss is acceptable; task loss is not.
        -->
        <property name="rejectedExecutionHandler">
            <bean class="java.util.concurrent.ThreadPoolExecutor.DiscardPolicy"/>
        </property>
    </bean>

    <!-- Enable @Scheduled only; @Async replaced by explicit executor injection -->
    <task:annotation-driven scheduler="retryScheduler"/>
    <task:scheduler id="retryScheduler" pool-size="2"/>

</beans>
